# RAIL Score MCP Server runtime dependencies.
# Pin exact versions at build time; ranges below track the official MCP SDK
# and a production ASGI stack.
mcp>=1.13.0,<2.0.0
httpx>=0.27.0,<1.0.0
pydantic>=2.7.0,<3.0.0
starlette>=0.37.0
uvicorn[standard]>=0.30.0
